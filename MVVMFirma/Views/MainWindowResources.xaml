<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:MVVMFirma.ViewModels"
                    xmlns:vw="clr-namespace:MVVMFirma.Views"
                    >
    
    <!-- DataTemplate section -->
    
    <DataTemplate DataType="{x:Type vm:WszystkieTowaryViewModel}">
        <vw:WszystkieTowaryView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NowyTowarViewModel}">
        <vw:NowyTowarView/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type vm:AllEmployeesViewModel}">
        <vw:AllEmployeesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewEmployeeViewModel}">
        <vw:NewEmployeeView/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type vm:AllAddressesViewModel}">
        <vw:AllAddressesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewAddressViewModel}">
        <vw:NewAddressView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllAislesViewModel}">
        <vw:AllAislesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewAisleViewModel}">
        <vw:NewAisleView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllSitesViewModel}">
        <vw:AllSitesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewSiteViewModel}">
        <vw:NewSiteView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllDepartmentsViewModel}">
        <vw:AllDepartmentsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewDepartmentViewModel}">
        <vw:NewDepartmentView/> 
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllGradesViewModel}">
        <vw:AllGradesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewGradeViewModel}">
        <vw:NewGradeView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllPositionsViewModel}">
        <vw:AllPositionsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewPositionViewModel}">
        <vw:NewPositionView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllManagersViewModel}">
        <vw:AllManagersView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewManagerViewModel}">
        <vw:NewManagerView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllPaymentMethodsViewModel}">
        <vw:AllPaymentMethodsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewPaymentMethodViewModel}">
        <vw:NewPaymentMethodView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllCategoriesViewModel}">
        <vw:AllCategoriesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewCategoryViewModel}">
        <vw:NewCategoryView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllClientsViewModel}">
        <vw:AllClientsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewClientViewModel}">
        <vw:NewClientView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllClientTypesViewModel}">
        <vw:AllClientTypesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewClientTypeViewModel}">
        <vw:NewClientTypeView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllLocationsViewModel}">
        <vw:AllLocationsView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewLocationViewModel}">
        <vw:NewLocationView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:AllInvoicesViewModel}">
        <vw:AllInvoicesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewInvoiceViewModel}">
        <vw:NewInvoiceView/>
    </DataTemplate>
    
    <DataTemplate DataType="{x:Type vm:AllInvoiceStatusesViewModel}">
        <vw:AllInvoiceStatusesView/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type vm:NewInvoiceStatusViewModel}">
        <vw:NewInvoiceStatusView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:SaleReportViewModel}">
        <vw:SalesReportView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:PayrollCostSummaryViewModel}">
        <vw:PayrollCostSummaryView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:InventorySearchViewModel}">
        <vw:InventorySearchView/>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:LoginWindowViewModel}">
        <vw:LoginWindowView/>
    </DataTemplate>


    <!-- Styles and Templates section -->

    <Style TargetType="TextBox">
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="true">
                <Setter Property="ToolTip" 
                        Value="{Binding RelativeSource={x:Static RelativeSource.Self},Path=(Validation.Errors)[0].ErrorContent}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- 
    The folllowing code is used to defines a style for toggle buttons 
    -->
    <Style x:Key="ModuleToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="MinWidth" Value="120"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Margin" Value="2,0"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#00587C"/>
        <Setter Property="Cursor" Value="Hand"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            Padding="8,4">

                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border"
                                    Property="Background"
                                    Value="#007A9E"/>
                        </Trigger>
                        
                        <!-- Active module -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border"
                                    Property="Background"
                                    Value="#004B5C"/>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border"
                                    Property="Opacity"
                                    Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- The following code defines a style for buttons -->
    <Style x:Key="MenuButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="#00587C"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Margin" Value="0,4"/>
        <Setter Property="BorderBrush" Value="LightGray"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="4" 
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="contentPresenter"
                                          Focusable="False"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#007A9E"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#004B5C"/>
                            <Setter TargetName="contentPresenter" Property="Margin" Value="1,1,0,0"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="Gray"/>
                            <Setter Property="Foreground" Value="LightGray"/>
                            <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

        <Style x:Key="MainHCCStyle" TargetType="{x:Type HeaderedContentControl}">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Border 
                        Background="#00587C" 
                        BorderBrush="LightGray" 
                        BorderThickness="1" 
                        CornerRadius="5" 
                        Margin="4" 
                        Padding="4" 
                        SnapsToDevicePixels="True">
                        <TextBlock 
                            FontSize="14"
                            FontWeight="Bold"
                            Foreground="White" 
                            HorizontalAlignment="Center"  
                            Text="{TemplateBinding Content}"/>
                    </Border>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <!-- 
                    This template ensures that content of a HeaderedContentControl 
                    fills the available vertical space. 
                -->
                <ControlTemplate TargetType="{x:Type HeaderedContentControl}">
                    <DockPanel>
                        <ContentPresenter 
                            DockPanel.Dock="Top"
                            ContentSource="Header" 
                            ContentTemplate="{TemplateBinding HeaderTemplate}"/>
                        <ContentPresenter 
                            ContentSource="Content" 
                            ContentTemplate="{TemplateBinding ContentTemplate}"/>
                    </DockPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ToolBar style for the main window -->
    <Style x:Key="MainToolBarStyle" TargetType="{x:Type ToolBar}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToolBar}">
                    <Border >
                        <DockPanel LastChildFill="True">
                            <ItemsPresenter VerticalAlignment="Center"/>
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Padding" Value="4"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Background" Value="#f3f3f3"/>
    </Style>
    
    <!--
  This style ensures that the borders in the main window are consistent.
  -->
    <Style x:Key="MainBorderStyle" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="WhiteSmoke" />
        <Setter Property="BorderBrush" Value="LightGray" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <!--
  This template explains how to render the list of commands on the left
  side in the main window (the 'Control Panel' area).
  -->
    <DataTemplate x:Key="CommandsTemplate">
        <ItemsControl IsTabStop="False" ItemsSource="{Binding}" Margin="6,2">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Style="{StaticResource MenuButtonStyle}"
                        Command="{Binding Path=Command}">
                        <TextBlock Text="{Binding Path=DisplayName}"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DataTemplate>

    <!-- 
  This template explains how to render 
  a tab item with a close button.
  -->
    <DataTemplate x:Key="ClosableTabItemTemplate">
        <DockPanel Width="120">
            <Button 
        Command="{Binding Path=CloseCommand}"
        Content="X"
        Cursor="Hand"
        DockPanel.Dock="Right"
        Focusable="False"
        FontFamily="Courier" 
        FontSize="9"
        FontWeight="Bold"  
        Margin="0,1,0,0"
        Padding="0"
        VerticalContentAlignment="Bottom"
        Width="16" Height="16" 
        />
            <ContentPresenter 
        Content="{Binding Path=DisplayName}" 
        VerticalAlignment="Center" 
        />
        </DockPanel>
    </DataTemplate>

    <!--
  This template explains how to render the 'Workspace' content area in the main window.
  -->
    <DataTemplate x:Key="WorkspacesTemplate">
        <TabControl 
      IsSynchronizedWithCurrentItem="True" 
      ItemsSource="{Binding}" 
      ItemTemplate="{StaticResource ClosableTabItemTemplate}"
      Margin="4"
      />
    </DataTemplate>
</ResourceDictionary>