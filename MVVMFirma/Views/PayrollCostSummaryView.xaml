<UserControl x:Class="MVVMFirma.Views.PayrollCostSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Payroll Cost Summary for Selected Period" HorizontalContentAlignment="Center"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="From:" Margin="3" HorizontalContentAlignment="Center"/>
            <DatePicker Grid.Column="1" Margin="3" SelectedDate="{Binding FromDate}"/>
            <Label Grid.Column="2" Content="To:" Margin="3" HorizontalContentAlignment="Center"/>
            <DatePicker Grid.Column="3" Margin="3" SelectedDate="{Binding ToDate}"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Select Department" Grid.Column="0" Margin="3"/>
            <ComboBox Grid.Column="1" Margin="3" 
                      ItemsSource="{Binding DepartmentsComboBoxItems}"
                      DisplayMemberPath="Value"
                      SelectedValuePath="Key"
                      SelectedValue="{Binding DepartmentId, Mode=TwoWay}"
                      />
        </Grid>
        
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Select Position" Grid.Column="0" Margin="3"/>
            <ComboBox Grid.Column="1" Margin="3" 
              ItemsSource="{Binding PositionsComboBoxItems}"
              DisplayMemberPath="Value"
              SelectedValuePath="Key"
              SelectedValue="{Binding PositionId, Mode=TwoWay}"
              IsEnabled="{Binding IsPositionEnabled}"
              />
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Select Employee" Grid.Column="0" Margin="3"/>
            <ComboBox Grid.Column="1" Margin="3" 
              ItemsSource="{Binding EmployeesComboBoxItems}"
              DisplayMemberPath="Value"
              SelectedValuePath="Key"
              SelectedValue="{Binding EmployeeId, Mode=TwoWay}"
              IsEditable="{Binding IsEmployeeEnabled}"
              />
        </Grid>

        <Grid Grid.Row="5">
            <Button Content="Calculate" Margin="3" Command="{Binding CalculateCommand}"/>
        </Grid>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Total payroll cost:" Grid.Column="0" Margin="3"/>
            <Label Grid.Column="1" Margin="3" HorizontalContentAlignment="Right" Content="{Binding PayrollCost}"/>
        </Grid>
    </Grid>
</UserControl>
