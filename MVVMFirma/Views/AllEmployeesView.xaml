<local:WszystkieViewBase x:Class="MVVMFirma.Views.AllEmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="1000">
    <Grid Background="White">
        <DataGrid 
            AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee Number" Binding="{Binding EmployeeNumber}"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}"/>
                <DataGridTextColumn Header="Sex" Binding="{Binding Sex}"/>
                <DataGridTextColumn Header="Date of Birth" Binding="{Binding DateOfBirth}"/>
                <DataGridTextColumn Header="Position" Binding="{Binding PositionName}"/>
                <DataGridTextColumn Header="Department" Binding="{Binding DepartmentName}"/>
                <DataGridTextColumn Header="Grade" Binding="{Binding GradeName}"/>
                <DataGridTextColumn Header="Hire date" Binding="{Binding HireDate}"/>
                <DataGridTextColumn Header="Termination date" Binding="{Binding TerminationDate, TargetNullValue='N/A'}"/>
                <DataGridTextColumn Header="Phone number" Binding="{Binding PhoneNumber}"/>
                <DataGridTextColumn Header="Email address" Binding="{Binding EmailAddress}"/>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}"/>
                <DataGridTextColumn Header="Manager Name" Binding="{Binding ManagerName}"/> 
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</local:WszystkieViewBase>
