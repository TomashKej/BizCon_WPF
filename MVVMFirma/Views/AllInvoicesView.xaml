<local:WszystkieViewBase x:Class="MVVMFirma.Views.AllInvoicesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MVVMFirma.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Invoice Number" Binding="{Binding InvoiceNumber}"/>
                <DataGridTextColumn Header="Client Name" Binding="{Binding ClientName}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding InvoiceStatusName}"/>
                <DataGridTextColumn Header="Issue Date" Binding="{Binding InvoiceDate, StringFormat=yyy-MM-dd}"/>
                <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat=yyy-MM-dd}"/>
                <DataGridTextColumn Header="Payment Date" Binding="{Binding PaymentDate, StringFormat=yyy-MM-dd, TargetNullValue='Not paid'}"/>
                <DataGridTextColumn Header="Total Amount" Binding="{Binding TotalAmount}"/>
                <DataGridTextColumn Header="Net Amount" Binding="{Binding NetAmount}"/>
                <DataGridTextColumn Header="Tax Amount" Binding="{Binding TaxAmount}"/>
                <DataGridTextColumn Header="Payment Method" Binding="{Binding PaymentMethodName}"/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</local:WszystkieViewBase>
