<UserControl x:Class="MVVMFirma.Views.SalesReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Width="400" HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="33"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="33"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Product Turnover for Selected Period" HorizontalContentAlignment="Center"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Label Grid.Column="0" Content="From:" Margin="3" HorizontalContentAlignment="Center"/>
            <DatePicker Grid.Column="1" Margin="3" SelectedDate="{Binding FromDate}"/>
            <Label Grid.Column="2" Content="To:" Margin="3" HorizontalContentAlignment="Center"/>
            <DatePicker Grid.Column="3" Margin="3" SelectedDate="{Binding ToDate}"/>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Select Product" Grid.Column="0" Margin="3"/>
            <ComboBox Grid.Column="1" Margin="3" 
                      ItemsSource="{Binding ProductsComboBoxItems}"
                      DisplayMemberPath="Value"
                      SelectedValuePath="Key"
                      SelectedValue="{Binding ProductId, Mode=TwoWay}"
                      />
        </Grid>
        <Button Grid.Row="3" Content="Calculate" Margin="3" Command="{Binding CalculateCommand}"/>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Total turnover:" Grid.Column="0" Margin="3"/>
            <Label Grid.Column="1" Margin="3" HorizontalContentAlignment="Right" Content="{Binding Revenue}"/>
        </Grid>
    </Grid>
    
</UserControl>
