<local:JedenViewBase x:Class="MVVMFirma.Views.NewPositionView"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
         xmlns:local="clr-namespace:MVVMFirma.Views"
         mc:Ignorable="d" 
         d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="White">
        <!--dwie kolumny i 6 wierszy-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="Position Name" Margin="0,8,0,8"/>
        <TextBox Grid.Column="1" Grid.Row="0" Margin="0,8,0,8" Text="{Binding Path=PositionName,UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"/>

        <Label Grid.Column="0" Grid.Row="1" Content="Department" Margin="0,8,0,8"/>
        <ComboBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding Departments}"
                  DisplayMemberPath="DepartmentName" SelectedValuePath="DepartmentId"
                  SelectedValue="{Binding SelectedDepartmentId, Mode=TwoWay, ValidatesOnDataErrors=True}"/>

        <Label Grid.Column="0" Grid.Row="2" Content="Grade" Margin="0,8,0,8"/>
        <ComboBox Grid.Column="1" Grid.Row="2" ItemsSource="{Binding Grades}"
                  DisplayMemberPath="GradeName" SelectedValuePath="GradeId"
                  SelectedValue="{Binding SelectedGradeId, Mode=TwoWay, ValidatesOnDataErrors=True}"/>

        <Label Grid.Column="0" Grid.Row="3" Content="Notes" Margin="0,8,0,8"/>
        <TextBox Grid.Column="1" Grid.Row="3" Margin="0,8,0,8" Text="{Binding Path=Notes,UpdateSourceTrigger=PropertyChanged}"/>


    </Grid>
</local:JedenViewBase>
